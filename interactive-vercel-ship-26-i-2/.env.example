# News Engine — set ONE of these to pull live India headlines.
# Without a key the engine returns clearly-labeled SAMPLE headlines (it still runs).
# Keys are read server-side only (in lib/news.ts) — never exposed to the browser.

# GNews (https://gnews.io) — free tier available
NEWS_API_KEY=

# newsapi.org — alternative provider
NEWSAPI_KEY=
